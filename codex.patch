diff --git a/PostListFront/posthub-ui/src/App.vue b/PostListFront/posthub-ui/src/App.vue
index 7bc641ae29fd30bd468b7e979d907d388ac4f77d..8c44e2b9caf6aa49739736c595f97f61f537b1c1 100644
--- a/PostListFront/posthub-ui/src/App.vue
+++ b/PostListFront/posthub-ui/src/App.vue
@@ -1,89 +1,187 @@
 <script setup>
 import { ref } from 'vue'
 import http from './api/http'
 import { ElMessage } from 'element-plus'
 import ArtsTable from './components/ArtsTable.vue'
 import RightPanel from './components/RightPanel.vue'
 
 const loading = ref(false)
 const items = ref([])
+const showContent = ref(false)
+
+const contentPanelId = 'central-panel'
 
 const loadData = async () => {
   loading.value = true
   try {
     const { data } = await http.get('/api/arts/')   // тот же эндпоинт
     // предполагаю структуру { items: [...] }
     items.value = Array.isArray(data?.items) ? data.items : []
     ElMessage.success('Загружено')
   } catch (e) {
     ElMessage.error('Ошибка загрузки')
   } finally {
     loading.value = false
   }
 }
+
+const toggleContent = () => {
+  showContent.value = !showContent.value
+}
 </script>
 
 <template>
   <div class="app-layout">
-    <div class="app-content">
-      <h1 class="app-title">posthub ui</h1>
+    <RightPanel />
+
+    <div class="content-shell">
+      <div class="content-transition-wrapper" :id="contentPanelId">
+        <transition name="content-fade" mode="out-in">
+          <div v-if="showContent" key="content" class="app-content">
+            <div class="content-controls">
+              <el-button
+                class="toggle-button"
+                type="primary"
+                plain
+                size="small"
+                :aria-expanded="showContent"
+                :aria-controls="contentPanelId"
+                @click="toggleContent"
+              >
+                Скрыть центральную панель
+              </el-button>
+            </div>
+
+            <h1 class="app-title">posthub ui</h1>
 
-      <el-button class="load-button" type="primary" :loading="loading" @click="loadData">
-        Загрузить данные с Django
-      </el-button>
+            <el-button class="load-button" type="primary" :loading="loading" @click="loadData">
+              Загрузить данные с Django
+            </el-button>
 
-      <div class="table-wrapper">
-        <ArtsTable :items="items" />
+            <div class="table-wrapper">
+              <ArtsTable :items="items" />
+            </div>
+          </div>
+
+          <div v-else key="placeholder" class="content-placeholder">
+            <el-button
+              class="toggle-button"
+              type="primary"
+              plain
+              size="small"
+              :aria-expanded="showContent"
+              :aria-controls="contentPanelId"
+              @click="toggleContent"
+            >
+              Открыть центральную панель
+            </el-button>
+          </div>
+        </transition>
       </div>
     </div>
-
-    <RightPanel />
   </div>
 </template>
 
 <style scoped>
 .app-layout {
   display: flex;
   min-height: 100vh;
   background: var(--el-bg-color-page);
+  align-items: stretch;
+}
+
+.content-shell {
+  flex: 1;
+  min-width: 0;
+  display: flex;
+  align-items: stretch;
+}
+
+.content-transition-wrapper {
+  flex: 1;
+  display: flex;
+  align-items: stretch;
 }
 
 .app-content {
   flex: 1;
   min-width: 0;
   padding: 32px 48px 48px;
   display: flex;
   flex-direction: column;
   align-items: center;
   gap: 28px;
   box-sizing: border-box;
 }
 
+.content-controls {
+  align-self: stretch;
+  display: flex;
+  justify-content: flex-start;
+}
+
+.content-placeholder {
+  flex: 1;
+  display: flex;
+  align-items: flex-start;
+  justify-content: flex-start;
+  padding: 32px 24px;
+  box-sizing: border-box;
+  background: var(--el-bg-color-page);
+}
+
+.content-placeholder .toggle-button {
+  margin-top: 12px;
+}
+
 .app-title {
   font-size: 40px;
   font-weight: 700;
   letter-spacing: 0.6px;
   text-transform: lowercase;
   margin: 0;
   text-align: center;
 }
 
 .load-button {
   align-self: center;
 }
 
 .table-wrapper {
   width: min(840px, 100%);
   align-self: stretch;
 }
 
+.toggle-button {
+  letter-spacing: 0.3px;
+}
+
+.content-fade-enter-active,
+.content-fade-leave-active {
+  transition: opacity 0.18s ease, transform 0.18s ease;
+}
+
+.content-fade-enter-from,
+.content-fade-leave-to {
+  opacity: 0;
+  transform: translateY(8px);
+}
+
 @media (max-width: 960px) {
   .app-layout {
     flex-direction: column;
   }
 
+  .content-shell {
+    width: 100%;
+  }
+
   .app-content {
     padding: 24px;
   }
+
+  .content-placeholder {
+    padding: 24px;
+  }
 }
 </style>
diff --git a/PostListFront/posthub-ui/src/components/RightPanel.css b/PostListFront/posthub-ui/src/components/RightPanel.css
index de749db9aa79a57a33f049c1477cbb6dba1465be..9eeaecb0f50b2cbcd2a57ccbc748799b1027976d 100644
--- a/PostListFront/posthub-ui/src/components/RightPanel.css
+++ b/PostListFront/posthub-ui/src/components/RightPanel.css
@@ -1,140 +1,259 @@
-/* Правая панель */
+@import url('https://fonts.googleapis.com/css2?family=Manrope:wght@500;600;700&display=swap');
+
+/* Панель с артами (теперь слева) */
 .right-panel {
-    flex: 0 0 320px;
-    width: 320px;
+    flex: 0 0 720px;
+    width: 720px;
     height: 100vh;
-    margin-left: auto;
+    margin: 0;
     background: var(--el-bg-color-page);
-    border-left: 1px solid var(--el-border-color);
+    border-right: 1px solid var(--el-border-color);
     overflow-y: auto;
-    padding: 16px 14px;
+    padding: 28px 24px;
     box-sizing: border-box;
+    font-family: 'Manrope', 'Inter', 'Segoe UI', sans-serif;
+    color: var(--el-text-color-primary);
+}
+
+.panel-header {
+    display: flex;
+    align-items: center;
+    justify-content: space-between;
+    margin-bottom: 18px;
+    padding-bottom: 14px;
+    border-bottom: 1px solid var(--el-border-color-light);
+}
+
+.panel-title {
+    font-size: 20px;
+    font-weight: 700;
+    letter-spacing: 0.3px;
+    margin: 0;
+    color: var(--el-text-color-primary);
+}
+
+.lock-button {
+    width: 38px;
+    height: 38px;
+    border-radius: 12px;
+    background: var(--el-fill-color-lighter);
+    border: 1px solid var(--el-border-color);
+    display: inline-flex;
+    align-items: center;
+    justify-content: center;
+    cursor: pointer;
+    transition: background 0.2s ease, border-color 0.2s ease, box-shadow 0.2s ease;
+    color: var(--el-text-color-secondary);
+    font-size: 18px;
+}
+
+.lock-button:hover {
+    background: var(--el-color-primary-light-9);
+    border-color: var(--el-color-primary-light-7);
+    color: var(--el-color-primary);
+}
+
+.lock-button:focus-visible {
+    outline: 2px solid var(--el-color-primary);
+    outline-offset: 2px;
+}
+
+.panel-list {
+    display: flex;
+    flex-direction: column;
+    gap: 12px;
 }
 
 /* Ровная сетка строки */
 .art-row {
     display: grid;
-    grid-template-columns: 2fr 0.8fr 1.05fr 1.05fr 1.05fr 0.9fr;
+    grid-template-columns: 3fr 0.95fr 1.1fr 1.1fr 1.1fr 0.9fr;
     align-items: center;
     gap: 10px;
-    padding: 10px 12px;
+    padding: 14px 16px;
     border-bottom: 1px solid var(--el-border-color-lighter);
     background: var(--el-bg-color);
     transition: background 0.15s ease, box-shadow 0.15s ease;
+    border-radius: 16px;
+    box-shadow: 0 0 0 1px transparent;
+}
+.art-row:hover {
+    background: var(--el-fill-color-light);
+    box-shadow: 0 0 0 1px var(--el-color-primary-light-7);
+}
+
+.right-panel.locked .art-row {
+    box-shadow: none;
+}
+
+.right-panel.locked .lock-button {
+    background: var(--el-fill-color);
+    border-color: var(--el-border-color);
+    color: var(--el-text-color-secondary);
 }
-.art-row + .art-row { margin-top: 6px; }
-.art-row:hover { background: var(--el-fill-color-light); }
 
-/* Название — центр, до 3 строк */
+/* Название — центр, до 4 строк */
 .art-name {
-    font-weight: 700;
+    font-weight: 600;
     font-size: 16px;
-    text-align: center;
-    line-height: 1.28;
+    text-align: left;
+    line-height: 1.32;
     word-break: break-word;
     display: -webkit-box;
-    -webkit-line-clamp: 3;
+    -webkit-line-clamp: 4;
     -webkit-box-orient: vertical;
     overflow: hidden;
 }
 
 .art-status {
     display: flex;
     justify-content: center;
 }
 
 /* Вертикальные столбики */
 .vstack {
     display: flex;
     flex-direction: column;
     align-items: center;
     gap: 6px;
     min-height: 40px;
     justify-content: center;
 }
 
-/* Текстовые бейджи */
-.label {
+.label-button {
     display: inline-flex;
     align-items: center;
     justify-content: center;
-    min-width: 52px;
-    padding: 4px 8px;
-    border-radius: 8px;
+    min-width: 56px;
+    padding: 6px 10px;
+    border-radius: 10px;
     background: var(--el-fill-color-lighter);
     color: var(--el-text-color-regular);
     font-size: 13px;
     font-weight: 500;
     line-height: 1.1;
     text-transform: lowercase;
     letter-spacing: 0.2px;
-    opacity: 0.45;
-    transition: opacity 0.15s ease, background 0.15s ease, color 0.15s ease;
+    opacity: 0.55;
+    border: 1px solid transparent;
+    cursor: pointer;
+    transition: opacity 0.18s ease, background 0.18s ease, color 0.18s ease, border-color 0.18s ease, transform 0.18s ease;
 }
-.label.active {
+
+.label-button:hover:not(:disabled) {
+    opacity: 0.85;
+    background: var(--el-fill-color);
+}
+
+.label-button:focus-visible {
+    outline: 2px solid var(--el-color-primary);
+    outline-offset: 2px;
+    opacity: 1;
+}
+
+.label-button:active:not(:disabled) {
+    transform: translateY(1px);
+}
+
+.label-button.active {
     opacity: 1;
     background: var(--el-color-primary-light-9);
     color: var(--el-color-primary);
+    border-color: var(--el-color-primary-light-7);
 }
 
-.status-pill {
+.label-button:disabled {
+    cursor: not-allowed;
+    opacity: 0.35;
+    background: var(--el-fill-color-lighter);
+    border-color: var(--el-border-color-lighter);
+    color: var(--el-text-color-placeholder);
+}
+
+.label-button:disabled:focus-visible {
+    outline: none;
+}
+
+.status-button {
     display: inline-flex;
     align-items: center;
     justify-content: center;
-    padding: 6px 12px;
+    padding: 7px 16px;
     border-radius: 999px;
     font-size: 13px;
     font-weight: 600;
     text-transform: lowercase;
     background: var(--el-fill-color-lighter);
     color: var(--el-text-color-primary);
     letter-spacing: 0.3px;
-    min-width: 96px;
-    text-align: center;
+    min-width: 112px;
+    border: 1px solid transparent;
+    cursor: pointer;
+    transition: background 0.18s ease, color 0.18s ease, border-color 0.18s ease, transform 0.18s ease;
+}
+
+.status-button:hover {
+    background: var(--el-fill-color);
 }
-.status-pill.done {
+
+.status-button:focus-visible {
+    outline: 2px solid var(--el-color-primary);
+    outline-offset: 2px;
+}
+
+.status-button:active {
+    transform: translateY(1px);
+}
+
+.status-button.done {
     background: var(--el-color-success-light-9);
     color: var(--el-color-success);
+    border-color: var(--el-color-success-light-5);
 }
 
 /* Чекбоксы Element Plus — БЕЗ :deep, так как CSS внешний (глобальный) */
 .art-posted .el-checkbox {
     --el-checkbox-font-size: 12px;
     margin: 0;
     display: flex;
     justify-content: center;
     align-items: center;
 }
 .art-posted .el-checkbox__inner {
     width: 16px;
     height: 16px;
 }
 .art-posted .el-checkbox__input { margin-right: 0; }
 
 @media (max-width: 960px) {
     .right-panel {
         flex: 0 0 auto;
         width: 100%;
         height: auto;
-        border-left: none;
-        border-top: 1px solid var(--el-border-color);
+        border-right: none;
+        border-bottom: 1px solid var(--el-border-color);
+        padding: 20px 16px;
     }
 }
 
 /* Микро-адаптив */
 @media (max-width: 1200px) {
     .art-row {
-        grid-template-columns: 1.9fr 0.8fr 1fr 1fr 1fr 0.9fr;
-        gap: 8px;
+        grid-template-columns: 2.6fr 0.9fr 1.05fr 1.05fr 1.05fr 0.9fr;
+        gap: 9px;
     }
-    .label {
-        min-width: 48px;
+    .label-button {
+        min-width: 50px;
         font-size: 12px;
-        padding: 3px 6px;
+        padding: 5px 8px;
     }
-    .status-pill {
-        min-width: 84px;
-        padding: 5px 10px;
+    .status-button {
+        min-width: 100px;
+        padding: 6px 12px;
         font-size: 12px;
     }
 }
+
+.right-panel.locked .label-button {
+    cursor: not-allowed;
+}
diff --git a/PostListFront/posthub-ui/src/components/RightPanel.vue b/PostListFront/posthub-ui/src/components/RightPanel.vue
index 21dfa0449a3698fc08be40634b9b0793935b3980..f82f3a1ca74161100f5465508ce7edb935a39c66 100644
--- a/PostListFront/posthub-ui/src/components/RightPanel.vue
+++ b/PostListFront/posthub-ui/src/components/RightPanel.vue
@@ -9,70 +9,210 @@ const arts = ref([
   {
     id: 1,
     name: 'Парочка из Финалки',
     status: 'complete',
     human: true,
     furry: false,
     sfw: true,
     nsfw: false,
     crop: false,
     post_targets: { twi16: true, twi18: false, bsky: true },
     posted: { twi16: true, twi18: false, bsky: false }
   },
   {
     id: 2,
     name: 'Тёмный рыцарь из Тарковы',
     status: 'in_progress',
     human: false,
     furry: true,
     sfw: false,
     nsfw: true,
     crop: true,
     post_targets: { twi16: false, twi18: true, bsky: true },
     posted: { twi16: false, twi18: false, bsky: false }
   }
 ])
+
+const isLocked = ref(false)
+
+const toggleLock = () => {
+  isLocked.value = !isLocked.value
+}
+
+const nextStatus = (art) => (art.status === 'complete' ? 'in_progress' : 'complete')
+
+const statusTitle = (art) => `Сменить статус на ${statusLabel(nextStatus(art))}`
+
+const toggleStatus = (art) => {
+  art.status = nextStatus(art)
+}
+
+const labelTitle = (label) =>
+  isLocked.value
+    ? `Панель заблокирована — разблокируйте, чтобы изменить ${label}`
+    : `Переключить ${label}`
+
+const toggleFlag = (art, key) => {
+  if (isLocked.value) return
+  art[key] = !art[key]
+}
+
+const toggleNestedFlag = (art, scope, key) => {
+  if (isLocked.value) return
+  art[scope][key] = !art[scope][key]
+}
 </script>
 
 <template>
-  <div class="right-panel">
-    <div v-for="art in arts" :key="art.id" class="art-row">
-      <!-- Название -->
-      <div class="art-name">{{ art.name }}</div>
-
-      <!-- Статус -->
-      <div class="art-status">
-        <span class="status-pill" :class="{ done: art.status === 'complete' }">
-          {{ statusLabel(art.status) }}
-        </span>
-      </div>
+  <div class="right-panel" :class="{ locked: isLocked }">
+    <div class="panel-header">
+      <h2 class="panel-title">Арты</h2>
+      <button
+        type="button"
+        class="lock-button"
+        :aria-pressed="isLocked"
+        :aria-label="isLocked ? 'Разблокировать панель' : 'Заблокировать панель'"
+        :title="isLocked ? 'Разблокировать панель' : 'Заблокировать панель'"
+        @click="toggleLock"
+      >
+        <span class="icon">{{ isLocked ? '🔒' : '🔓' }}</span>
+      </button>
+    </div>
 
-      <!-- Тип персонажа -->
-      <div class="art-type vstack">
-        <span class="label" :class="{ active: art.human }">human</span>
-        <span class="label" :class="{ active: art.furry }">furry</span>
-      </div>
+    <div class="panel-list">
+      <div v-for="art in arts" :key="art.id" class="art-row">
+        <!-- Название -->
+        <div class="art-name">{{ art.name }}</div>
 
-      <!-- Контент -->
-      <div class="art-content vstack">
-        <span class="label" :class="{ active: art.sfw }">sfw</span>
-        <span class="label" :class="{ active: art.nsfw }">nsfw</span>
-        <span class="label" :class="{ active: art.crop }">crop</span>
-      </div>
+        <!-- Статус -->
+        <div class="art-status">
+          <button
+            type="button"
+            class="status-button"
+            :class="{ done: art.status === 'complete' }"
+            :aria-pressed="art.status === 'complete'"
+            :aria-label="statusTitle(art)"
+            :title="statusTitle(art)"
+            @click="toggleStatus(art)"
+          >
+            {{ statusLabel(art.status) }}
+          </button>
+        </div>
 
-      <!-- Куда постить (план) -->
-      <div class="art-post vstack">
-        <span class="label" :class="{ active: art.post_targets.twi16 }">twi16</span>
-        <span class="label" :class="{ active: art.post_targets.twi18 }">twi18</span>
-        <span class="label" :class="{ active: art.post_targets.bsky }">bsky</span>
-      </div>
+        <!-- Тип персонажа -->
+        <div class="art-type vstack">
+          <button
+            type="button"
+            class="label-button"
+            :class="{ active: art.human }"
+            :disabled="isLocked"
+            :aria-pressed="art.human"
+            :aria-label="labelTitle('human')"
+            :title="labelTitle('human')"
+            @click="toggleFlag(art, 'human')"
+          >
+            human
+          </button>
+          <button
+            type="button"
+            class="label-button"
+            :class="{ active: art.furry }"
+            :disabled="isLocked"
+            :aria-pressed="art.furry"
+            :aria-label="labelTitle('furry')"
+            :title="labelTitle('furry')"
+            @click="toggleFlag(art, 'furry')"
+          >
+            furry
+          </button>
+        </div>
+
+        <!-- Контент -->
+        <div class="art-content vstack">
+          <button
+            type="button"
+            class="label-button"
+            :class="{ active: art.sfw }"
+            :disabled="isLocked"
+            :aria-pressed="art.sfw"
+            :aria-label="labelTitle('sfw')"
+            :title="labelTitle('sfw')"
+            @click="toggleFlag(art, 'sfw')"
+          >
+            sfw
+          </button>
+          <button
+            type="button"
+            class="label-button"
+            :class="{ active: art.nsfw }"
+            :disabled="isLocked"
+            :aria-pressed="art.nsfw"
+            :aria-label="labelTitle('nsfw')"
+            :title="labelTitle('nsfw')"
+            @click="toggleFlag(art, 'nsfw')"
+          >
+            nsfw
+          </button>
+          <button
+            type="button"
+            class="label-button"
+            :class="{ active: art.crop }"
+            :disabled="isLocked"
+            :aria-pressed="art.crop"
+            :aria-label="labelTitle('crop')"
+            :title="labelTitle('crop')"
+            @click="toggleFlag(art, 'crop')"
+          >
+            crop
+          </button>
+        </div>
 
-      <!-- Уже запощено (факт) -->
-      <div class="art-posted vstack">
-        <el-checkbox v-model="art.posted.twi16" size="small" />
-        <el-checkbox v-model="art.posted.twi18" size="small" />
-        <el-checkbox v-model="art.posted.bsky" size="small" />
+        <!-- Куда постить (план) -->
+        <div class="art-post vstack">
+          <button
+            type="button"
+            class="label-button"
+            :class="{ active: art.post_targets.twi16 }"
+            :disabled="isLocked"
+            :aria-pressed="art.post_targets.twi16"
+            :aria-label="labelTitle('twi16')"
+            :title="labelTitle('twi16')"
+            @click="toggleNestedFlag(art, 'post_targets', 'twi16')"
+          >
+            twi16
+          </button>
+          <button
+            type="button"
+            class="label-button"
+            :class="{ active: art.post_targets.twi18 }"
+            :disabled="isLocked"
+            :aria-pressed="art.post_targets.twi18"
+            :aria-label="labelTitle('twi18')"
+            :title="labelTitle('twi18')"
+            @click="toggleNestedFlag(art, 'post_targets', 'twi18')"
+          >
+            twi18
+          </button>
+          <button
+            type="button"
+            class="label-button"
+            :class="{ active: art.post_targets.bsky }"
+            :disabled="isLocked"
+            :aria-pressed="art.post_targets.bsky"
+            :aria-label="labelTitle('bsky')"
+            :title="labelTitle('bsky')"
+            @click="toggleNestedFlag(art, 'post_targets', 'bsky')"
+          >
+            bsky
+          </button>
+        </div>
+
+        <!-- Уже запощено (факт) -->
+        <div class="art-posted vstack">
+          <el-checkbox v-model="art.posted.twi16" size="small" />
+          <el-checkbox v-model="art.posted.twi18" size="small" />
+          <el-checkbox v-model="art.posted.bsky" size="small" />
+        </div>
       </div>
     </div>
   </div>
 </template>
-
