diff --git a/PostListFront/posthub-ui/src/App.vue b/PostListFront/posthub-ui/src/App.vue
index 7bc641ae29fd30bd468b7e979d907d388ac4f77d..8589c507beda13d8414b13b48ef3519c32472cf3 100644
--- a/PostListFront/posthub-ui/src/App.vue
+++ b/PostListFront/posthub-ui/src/App.vue
@@ -1,89 +1,167 @@
 <script setup>
 import { ref } from 'vue'
 import http from './api/http'
 import { ElMessage } from 'element-plus'
 import ArtsTable from './components/ArtsTable.vue'
 import RightPanel from './components/RightPanel.vue'
 
 const loading = ref(false)
 const items = ref([])
+const showContent = ref(false)
 
 const loadData = async () => {
   loading.value = true
   try {
     const { data } = await http.get('/api/arts/')   // тот же эндпоинт
     // предполагаю структуру { items: [...] }
     items.value = Array.isArray(data?.items) ? data.items : []
     ElMessage.success('Загружено')
   } catch (e) {
     ElMessage.error('Ошибка загрузки')
   } finally {
     loading.value = false
   }
 }
+
+const toggleContent = () => {
+  showContent.value = !showContent.value
+}
 </script>
 
 <template>
   <div class="app-layout">
-    <div class="app-content">
-      <h1 class="app-title">posthub ui</h1>
+    <RightPanel />
 
-      <el-button class="load-button" type="primary" :loading="loading" @click="loadData">
-        Загрузить данные с Django
-      </el-button>
+    <div class="content-shell">
+      <transition name="content-fade" mode="out-in">
+        <div v-if="showContent" key="content" class="app-content">
+          <div class="content-controls">
+            <el-button
+              class="toggle-button"
+              type="primary"
+              plain
+              size="small"
+              @click="toggleContent"
+            >
+              Скрыть центральную панель
+            </el-button>
+          </div>
 
-      <div class="table-wrapper">
-        <ArtsTable :items="items" />
-      </div>
-    </div>
+          <h1 class="app-title">posthub ui</h1>
 
-    <RightPanel />
+          <el-button class="load-button" type="primary" :loading="loading" @click="loadData">
+            Загрузить данные с Django
+          </el-button>
+
+          <div class="table-wrapper">
+            <ArtsTable :items="items" />
+          </div>
+        </div>
+
+        <div v-else key="placeholder" class="content-placeholder">
+          <el-button class="toggle-button" type="primary" plain size="small" @click="toggleContent">
+            Открыть центральную панель
+          </el-button>
+        </div>
+      </transition>
+    </div>
   </div>
 </template>
 
 <style scoped>
 .app-layout {
   display: flex;
   min-height: 100vh;
   background: var(--el-bg-color-page);
+  align-items: stretch;
+}
+
+.content-shell {
+  flex: 1;
+  min-width: 0;
+  display: flex;
+  align-items: stretch;
 }
 
 .app-content {
   flex: 1;
   min-width: 0;
   padding: 32px 48px 48px;
   display: flex;
   flex-direction: column;
   align-items: center;
   gap: 28px;
   box-sizing: border-box;
 }
 
+.content-controls {
+  align-self: stretch;
+  display: flex;
+  justify-content: flex-start;
+}
+
+.content-placeholder {
+  flex: 1;
+  display: flex;
+  align-items: flex-start;
+  justify-content: flex-start;
+  padding: 32px 24px;
+  box-sizing: border-box;
+  background: var(--el-bg-color-page);
+}
+
+.content-placeholder .toggle-button {
+  margin-top: 12px;
+}
+
 .app-title {
   font-size: 40px;
   font-weight: 700;
   letter-spacing: 0.6px;
   text-transform: lowercase;
   margin: 0;
   text-align: center;
 }
 
 .load-button {
   align-self: center;
 }
 
 .table-wrapper {
   width: min(840px, 100%);
   align-self: stretch;
 }
 
+.toggle-button {
+  letter-spacing: 0.3px;
+}
+
+.content-fade-enter-active,
+.content-fade-leave-active {
+  transition: opacity 0.18s ease, transform 0.18s ease;
+}
+
+.content-fade-enter-from,
+.content-fade-leave-to {
+  opacity: 0;
+  transform: translateY(8px);
+}
+
 @media (max-width: 960px) {
   .app-layout {
     flex-direction: column;
   }
 
+  .content-shell {
+    width: 100%;
+  }
+
   .app-content {
     padding: 24px;
   }
+
+  .content-placeholder {
+    padding: 24px;
+  }
 }
 </style>
diff --git a/PostListFront/posthub-ui/src/components/RightPanel.css b/PostListFront/posthub-ui/src/components/RightPanel.css
index de749db9aa79a57a33f049c1477cbb6dba1465be..0492fd65a14f77b11d65f6f234a0f1041a629312 100644
--- a/PostListFront/posthub-ui/src/components/RightPanel.css
+++ b/PostListFront/posthub-ui/src/components/RightPanel.css
@@ -1,35 +1,35 @@
-/* Правая панель */
+/* Панель с артами (теперь слева) */
 .right-panel {
-    flex: 0 0 320px;
-    width: 320px;
+    flex: 0 0 480px;
+    width: 480px;
     height: 100vh;
-    margin-left: auto;
+    margin: 0;
     background: var(--el-bg-color-page);
-    border-left: 1px solid var(--el-border-color);
+    border-right: 1px solid var(--el-border-color);
     overflow-y: auto;
-    padding: 16px 14px;
+    padding: 20px 18px;
     box-sizing: border-box;
 }
 
 /* Ровная сетка строки */
 .art-row {
     display: grid;
     grid-template-columns: 2fr 0.8fr 1.05fr 1.05fr 1.05fr 0.9fr;
     align-items: center;
     gap: 10px;
     padding: 10px 12px;
     border-bottom: 1px solid var(--el-border-color-lighter);
     background: var(--el-bg-color);
     transition: background 0.15s ease, box-shadow 0.15s ease;
 }
 .art-row + .art-row { margin-top: 6px; }
 .art-row:hover { background: var(--el-fill-color-light); }
 
 /* Название — центр, до 3 строк */
 .art-name {
     font-weight: 700;
     font-size: 16px;
     text-align: center;
     line-height: 1.28;
     word-break: break-word;
     display: -webkit-box;
@@ -94,47 +94,47 @@
 }
 .status-pill.done {
     background: var(--el-color-success-light-9);
     color: var(--el-color-success);
 }
 
 /* Чекбоксы Element Plus — БЕЗ :deep, так как CSS внешний (глобальный) */
 .art-posted .el-checkbox {
     --el-checkbox-font-size: 12px;
     margin: 0;
     display: flex;
     justify-content: center;
     align-items: center;
 }
 .art-posted .el-checkbox__inner {
     width: 16px;
     height: 16px;
 }
 .art-posted .el-checkbox__input { margin-right: 0; }
 
 @media (max-width: 960px) {
     .right-panel {
         flex: 0 0 auto;
         width: 100%;
         height: auto;
-        border-left: none;
-        border-top: 1px solid var(--el-border-color);
+        border-right: none;
+        border-bottom: 1px solid var(--el-border-color);
     }
 }
 
 /* Микро-адаптив */
 @media (max-width: 1200px) {
     .art-row {
         grid-template-columns: 1.9fr 0.8fr 1fr 1fr 1fr 0.9fr;
         gap: 8px;
     }
     .label {
         min-width: 48px;
         font-size: 12px;
         padding: 3px 6px;
     }
     .status-pill {
         min-width: 84px;
         padding: 5px 10px;
         font-size: 12px;
     }
 }
